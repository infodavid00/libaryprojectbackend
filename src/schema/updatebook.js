export default function Bookupdateschema(
  imageurl,
  bookname,
  ratings,
  authorname,
  category
) {
  this.imageurl = imageurl;
  this.bookname = bookname;
  this.ratings = ratings;
  this.authorname = authorname;
  this.category = category;
}
